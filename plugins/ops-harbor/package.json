{
  "name": "plugin-ops-harbor",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "oxlint",
    "check:fix": "oxlint --fix",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@repo/ts-config": "workspace:*",
    "@repo/vitest-config": "workspace:*",
    "@vitest/coverage-v8": "^3.2.0",
    "vitest": "^3.2.0"
  }
}
