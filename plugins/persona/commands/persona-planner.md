---
description: ペルソナを planner に切り替える
---

!mkdir -p "$CLAUDE_PROJECT_DIR/.agents/tmp/persona" && echo planner > "$CLAUDE_PROJECT_DIR/.agents/tmp/persona/$CLAUDE_CODE_SESSION_ID"

ペルソナを **planner** に切り替えた（このセッションのみ）。次のプロンプトから planner として振る舞う。
