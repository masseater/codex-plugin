{
  "name": "plugin-progress-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "oxlint",
    "check:fix": "oxlint --fix",
    "typecheck": "tsgo --noEmit"
  },
  "devDependencies": {
    "@repo/ts-config": "*",
    "@repo/vitest-config": "*",
    "@vitest/coverage-v8": "^3.2.0",
    "vitest": "^3.2.0"
  }
}
